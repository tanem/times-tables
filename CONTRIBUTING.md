# Contributing

Issues are welcome: a bug, a question or a suggestion.

Pull requests are welcome once an issue has agreed the change. `npm run check` must pass, commits follow [Conventional Commits](https://www.conventionalcommits.org/) (`type: description`), and pull requests merge with a merge commit, so every commit on the branch lands on `main` as it is.
