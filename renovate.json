{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "automergeStrategy": "merge-commit",
  "extends": [
    "config:js-app",
    ":automergeAll",
    ":label(internal)",
    ":dependencyDashboard",
    ":enableVulnerabilityAlerts",
    ":semanticCommits",
    "helpers:pinGitHubActionDigests"
  ],
  "lockFileMaintenance": {
    "enabled": true
  },
  "minimumReleaseAge": "3 days",
  "packageRules": [
    {
      "description": "Lock file maintenance, pin and replacement updates have no release timestamp, so requiring one would hold them as pending forever. The 3-day age still applies to the npm install that writes the lock file.",
      "matchUpdateTypes": ["lockFileMaintenance", "pin", "replacement"],
      "minimumReleaseAgeBehaviour": "timestamp-optional"
    },
    {
      "description": "typescript-eslint does not yet support TypeScript 7. Remove this once it does.",
      "matchPackageNames": ["typescript"],
      "allowedVersions": "<7"
    }
  ]
}
